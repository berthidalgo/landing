# Entorno y secretos
.env
.env.*
!.env.example

# Vercel
.vercel/

# Material de trabajo que no va al repo público:
# másteres de proveedor, capturas de referencia, material con marca de agua.
PROWIN/
*.mp4
*.mov

# …salvo los vídeos ya preparados para web: recomprimidos, sin marca de agua
# de terceros y por debajo de 6 MB. Son los que la landing sirve de verdad.
!img/bucle.mp4
!img/demo.mp4
!img/demo2.mp4

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/

# Dependencias (por si algún día hace falta un build)
node_modules/
